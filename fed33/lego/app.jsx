export default function lego(params) {
    let application = {
        title: "Lego",
        description: "A simple lego loader",
        DOCUMENT: 'sample doc'
    }

    console.log(application);
    return application;
}